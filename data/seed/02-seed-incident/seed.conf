-- The Memphis DC Meltdown (US) / Brno DC Meltdown (CZ) — frozen parameters.
-- C-3a spec v2 (project/kantheon/design/demo-tpcds/03-c-data-options.md) + Appendix B (Q-4).
-- Physical warehouse_sk/reason_sk are shared across both worlds (same pristine origin, same
-- redate — only display text/currency differs per world, per BM-7 "mirrored, not re-invented").
-- DO NOT re-tune per world: the % figures are frozen once (US), CZ inherits them verbatim.

\set meltdown_sk 5
\set late_reason_sk 3
\set year 2025

\set s1_week_start 31
\set s1_week_end 47
\set s1_pct 70

\set s2_week_start 32
\set s2_week_end 48
\set s2_pct 60

\set s3_week_start 33
\set s3_week_end 49
\set s3_pct 40

-- Q-4 resolution (frozen at seed build, 2026-07-18, applying the spec'd 60% literally):
-- catalog H2 2025 YoY = -8.44% (matches Appendix B's -8.4% prediction for a 60% deletion,
-- short of the "-10..12%" H2 headline as Appendix B itself flagged); November 2025 YoY =
-- -12.45% (hits the spec'd Nov target). Per Appendix B's own suggested resolution: brief the
-- KPI as the November / weeks-32-48 figure, not a full-H2 "-10..12%" headline. Full R0 table
-- freezes in Stage 1.6.
