# TTR-M build artifacts (regenerated)
dist/
node_modules/
.modeler/
*.log

# Python (data/ generators and their test suites)
__pycache__/
*.pyc
.venv/
.pytest_cache/

# data build scratch (dumps live in SeaweedFS tpcds-staging/hartland/{us,cz}/, not in git)
data/**/*.dump
data/**/*.sql.gz
data/recon/results/*.tmp

# model build scratch — hermetic physical-schema reference pulled from the live DB
# (Stage 2.1 pre-flight); regenerate with `psql -c "\d+ <table>"`, don't commit.
model/db/.schema-ref/
